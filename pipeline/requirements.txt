# Core geospatial
rasterio>=1.3
pysheds>=0.4
gdal>=3.6
geopandas>=0.14
shapely>=2.0
fiona>=1.9
pyproj>=3.6

# HEC-RAS automation
ras-commander>=0.90   # 0.90+ adds RasPreprocess.preprocess_plan() for Windows HEC-RAS preprocessing (Bill K. commit 8c0c1c8); tested baseline 0.93.0 (2026-04-07)
h5py>=3.9

# Post-processing — cloud-native GIS export (optional)
ras2cng>=0.5.0         # HEC-RAS → GeoParquet + PMTiles + DuckDB (Bill Katzenmeyer / CLB Engineering, MIT)

# USGS data
streamstats-access>=0.1
dataretrieval>=1.0

# Numerical
numpy>=1.24,<2.0  # pysheds 0.5 uses np.in1d removed in NumPy 2.0; pin until pysheds fixes upstream
scipy>=1.11
pandas>=2.0

# Web / API
fastapi>=0.110
uvicorn>=0.27
httpx>=0.26
requests>=2.31
pydantic>=2.0

# Utilities
python-dotenv>=1.0
tqdm>=4.66
click>=8.1
loguru>=0.7

# Cloud storage (optional — R2 upload)
boto3>=1.34
